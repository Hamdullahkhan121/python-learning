import random
# Generate an array of length 14 with random numbers between 60 and 150
random_array = [random.randint(60, 150) for _ in range(14)]

# You can now use random_array as hardcoded data in your code
print(random_array)